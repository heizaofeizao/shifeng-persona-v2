# 版本沿革（CHANGELOG）

> **为什么单独成文件**：这些历史条目每轮都会被注入却几乎不被使用，曾让核心卡 `SKILL.md` 从 20 KB 涨到 38 KB（占常驻成本 1/3）。**核心卡只保留最新版本摘要 + 指向本文件的指针**，历史细节下沉到这里按需查阅。
>
> 倒序排列（最新在上）。每条记录的是**为什么改**——起因、实测证据、被否掉的做法，比"改了什么"更值得留存。

---

## v3.3（2026-09-20）· 冗余排查与精简

- **起因**：用户要求"排查这个 skill 的冗余项，合并精简内容"。用两个脚本做数据驱动的检测（段落级完全重复扫描 + 规则主题 × 文件矩阵），结论：**字面重复极少（只有 Python 样板），冗余全在"同一规则多处表述"与"历史产物堆积"**。
- **核心卡瘦身 47%**（38,735 B → 20,390 B，行数 354 → 272）：
  - **版本沿革外移** → 本文件。历史条目每轮都被注入却几乎不被使用，曾占核心卡约 33%。核心卡只留最新一条摘要 + 指针。
  - **`description` 压回 100 字符内**（原 308 字符，违反用户既有铁律）。完整触发词下沉到正文「触发词全集」。
  - **「安全护栏」与 `qc.md` §三 逐条重复** → 核心卡只留最高优先级 3 条 + 指针。
- **「误判红线」三处归一**：原散在 `style-short.md` §十 / `satire-decoder.md` / `stance-library.md`。现以 **`style-short.md` §十 为唯一权威版**（并补入原 satire-decoder 的「反向红线：劝学工科」），另两处改为指针。
- **「交付前自检清单」两份合一**：原 `style-short.md` §七 与 `qc.md` §四 各存一份（分层版 + 优先级版），是最大的规则漂移源。现 **`qc.md` §四 为唯一完整清单**（吸收原 §七 的全部逐层阈值），`style-short.md` §七 只留短答特有项 + 指针。
- **`style-short.md` §九 去重**：与 §四「七种短反讽手法」重叠，压为"§四 未含的两类"（因 `style-invective.md` 引用了 §九 编号，保留节号）。
- **历史产物归档**：`AUDIT_2026-09-19.md` → `docs/archive/`；8 个历轮测试件 → `tests/archive/`；新增 `tests/README.md`（测试分工原则）与 `docs/README.md`。**均不参与回归断言**，归档零风险。删去装饰性空文件 `assets/.gitkeep`（`avatar.jpg` 已在）。
- **顺手订正过期引用**：核心卡索引原写 `tests/golden_cases.md` 为"A–G 组"，实际已扩到 K 组。

**未做（需决策）**：① `references/research/` 6 个底稿与 modules 有主题重叠，但属懒加载（不占常驻），合并收益低而改动大；② 4 个脚本的样板代码（stdout 编码 / `HERE` / argparse）可抽 `_common.py`，但收益仅数 KB 而需重跑全部引擎冒烟。

## v3.2（2026-09-20）· L2b 欧式长句 + L2c 反口语化

- **起因**：用户对四篇定稿的诊断——「**还是缺乏欧式长句**」「**过于口语化，石枫不喜欢口语化**」。回语料实测后两条**全部证实**，且证实方式比预想更精确。
- **缺口实测（四稿 vs mid 基线）**：长复句率 14~20%（**达标**），而 **关联词配套 0 / 的-定语链 0 / 分号长复句 0 —— 四篇全部挂零**。→ 句子**长度够、肌理不对**：他是靠「关联词嵌套 + 长定语 + 名词化」把句子撑长的，稿子是靠「逗号串短分句」撑长的。**长度相同，句法血统不同。**
- 新增 `modules/style-euro.md`：**六装置**——① 的-定语链（1.26/千字，35.5% 篇）② 关联词配套（16 对，含 如果…就/当…时/无论…都/不是…而是…）③ 名词化（…性/…化/对…的X）④ 关系化定语（那些…的人）⑤ 分号长复句 ⑥ 破折号插入成分。含**他本人的 7 条欧式长句范例**（已剔译稿核验）与 3 组「口语流水句 → 欧化长句」改写对照。
- **关键辨析**：**欧化 ≠ 作文腔**。L3 禁的是横向并列的机械分层（首先/其次/综上，语料≈0），L2b 要的是纵向嵌套的关联词（语料 1.4~4.0/千字）。**去作文腔时最容易连欧化一起铲掉**——铲完就成了格言体，那是另一种不像。
- **L2c 反口语化**：硬禁 26 条（每条**逐条回语料核验，篇占比 ≤0.25%**，其中 `你去…就明白` / `哪门子` / `组合拳` / `摊上` / `反正吧` 等 13 条为**零命中**）；句尾语气词**只禁 呗啦哦哟咯嗯呀**（全语料合计仅 23 次 / 24302 句），而 `呢`(585)/`吗`(531)/`吧`(107)/`啊`(69)/`嘛`(48) **一律放行**——`呢？`是他的签名反问尾巴，**禁错等于把他最像的地方改掉**。
- `scripts/style_check.py` 升级为 **v5**：新增 `euro()` / `colloquial()` 两函数 + `EURO_BASE`（**以下限为主**，欠界即告警；溢界上限放宽到 p90×1.5~2——**宁可略过他，不要欠他**）+ `COLLOQ_BAN` / `COLLOQ_TAIL_BAN` / `COLLOQ_DOSE` / `COLLOQ_CAP` 四张表。密度上限给「最短允许 3 处」折算，避免用密度尺误伤 450 字的中篇。
- **两处闸门自身缺陷由新守卫抓出**：① 名词化原用「…性/…化」通配，把 Q3 的「共同**体文化**，」判成名词化 → 改为**白名单**（34 词 + 对…的X）；② 硬禁表原**分组**（8 个北方口语词并一条），回归守卫按「每条 ≤0.5%」校验时分组必然超标（11 篇 = 0.92%）→ **一律拆成单词条**便于逐条审计。另有 L2b 非空转探针因缺「的-定语链」而误 FAIL，也一并修正。
- `modules/style-diction.md`：**订正一处过期断言**（§六 反面清单原把 赋能/闭环/破局/生态位 列为"他不用"，与 v3.1 已确认的"他真用过"矛盾）+ 挂 L2b 指针（"长度够≠肌理对"）+ 自检清单补 3 项。
- 回归断言 71 → **82**（+6 文件/规则存在性 + 1 口语表语料守卫 + 2 非空转验证）。四篇定稿在 v5 闸门下**全部 0 告警**，L2b 四指标由全零提升至 配套 1.8~6.4 / 的链 1.8~2.1 / 名词化 1.8~6.4 / 分号 1~2 处。测试件 `tests/archive/test_fresh4_2026-09-20.md`（v3.2 节）。

## v3.1（2026-09-20）· 修闸门自身的两处误判 + 防漂移断言

- **起因**：用四道全新题实跑 v3.0，闸门把两句**本来正确**的话判成问题，同时漏掉一处**真问题**。
- **误报 1 · 互联网黑话表**：`UNWANTED` 原 8 词，回语料逐词核验发现 **4 个是他真用过的**——生态位 10 篇 / 赋能 2 篇 / 闭环 2 篇 / 破局 1 篇。Q1 初稿的"巫师、祭司那个生态位"因此被误判，照单全收就会改掉一句真话。已修表（只留 4 个零命中词）并加注释。
- **漏报 2 · 论战靶子词表**：`INV_TARGET` 只收长尾靶子，漏掉高频词 **大V 8 篇 / 中产 53 篇 / 公众号 22 篇 / 体面人 5 篇 / 博主 3 篇**，"某些体面大V"竟完全未被识别为开火；且 `--invective` 强制模式下四拍全空仍打印"OK 四拍齐备"，**结论与事实相反**。已补词表（+ `INV_REVERSE` 补 `守夜人`），并把提示语改为分情形输出、明示"词表为词形匹配，标 `—` 不代表内容缺失"。
- **新增防漂移断言**（71/71）：`UNWANTED` 每个词都必须回语料零命中——**把"黑名单误加入他说过的词"这个 bug 钉死**。
- 方法论沉淀：**黑名单是"他没说过"的断言，加词前必须先 grep 语料**；否则闸门会把你骗去改本来对的地方。

## v3.0（2026-09-19）· L4 论战层：德性攻击四拍

- **起因**：用户指出"除了对左右分界的不满，回答中他往往还会对某些自己不喜欢的博主进行攻击——认为其沉浸在全球化黄金时代的美梦中，自以为享受现代生活但实际上缺乏德行导致实际上在破坏现代生活。尤其在目田、现代生活、引用立场和他不同的人等话题下。"该描述经全量语料实测**完全证实**（1660 篇本人原创 → 命中 111 句 → 核心句式 20 句）。
- 新增 `modules/style-invective.md`：**四拍结构**——`点靶（贬称+后缀）→ 揭梦（拆穿时代幻觉）→ 论德（跳过政策直指德性亏空）→ 反转（你以为你在享受，其实你在寄食并拆毁它）`。**第 4 拍是灵魂**。含靶子谱系、五个修辞装置（**享受物清单法 / 拟神反讽 / 引号包抄 / 延迟落刀 / 划线二分**）、六条红线，以及 14 条**经回查核验的真实原话**（含 `pin/1836071567567765506` "秩序的寄生虫"、`answer/1943116425733572071` "伟大的进步之神"、`answer/3108652164` "原子化的错觉"、`answer/3249008621` 指名评"西塞罗先生"）。
- `scripts/style_check.py` 升级为 **v4**：新增 `invective()` 四拍自检 + `INV_TARGET/DREAM/VIRTUE/REVERSE/LIST` 五张词表 + `--invective` 强制输出；**含靶子词时自动启动**。闸门逻辑经非空转验证——纯点名骂（无德性/反转拍）**如期告警 2 条**，四拍齐备稿 **0 条论战告警**。
- `modules/style-short.md` §七 新增论战闸门条目；`modules/vocab.md` §一 交叉引用本模块。
- 回归断言 63 → **70**（+1 文件 +5 规则）。

## v2.9（2026-09-19）· 讲解冒号指标 + 结尾闸门 + mid 档基线

- **起因**：用户在 v2.8 定稿后指出"**他几乎不用冒号，回答也不会有最后那一句话**"。实测证实两条：
- **讲解冒号**——拆解全语料 2391 个冒号后发现，其中 19.1% 是引述冒号、17.0% 是贴链接/贴图/标题冒号，真正行文讲解用的只占 61%。按单篇分位看，**短答 82% 的篇目一个都没有（中位 0）、中答中位数同为 0（p90 才 3.6/千字）**。v2.8 定稿里的 3 处讲解冒号（`挺耐人寻味：它预设了…`、`绕不开两条：人性上…`）全部超标。
- **结尾**——实测 886 篇多段文档：末句为疑问 35.9%、**末句含升华/总结词仅 4.6%**（短答 0.9% / pin 1.4%）。他的结尾只有四形态（平实泄气 / 狠话直落 / 疑问悬停 / 口语延续），**从不回扣开头、从不发出号召**。v2.8 定稿的收尾"先把那把歪掉的坐标扶正，再谈转不转吧"正是典型作文式闭合。
- **顺带修掉一个方法论缺陷**：`style_check.py` 原只有 short/pin/long 三档，**四段式回答（300~800 字）没有对应基线**，一直被拿短答的尺子量，产生 3 处误伤告警。新增 **mid 档**（247 条实测，区间取单篇 p10~p90），并让档位**按字数自动选择**。
- `scripts/style_check.py` 升级为 v3：新增 `lecture_colons()`（只计讲解冒号，引述/链接/标题/枚举均排除）、`ending()`（三类结尾雷区：升华词 / 「……吧」号召 / 末段复现开头 4 字短语）、`auto_profile()`；L1 增「讲解冒号」指标，四档基线全部重算为单篇 p10~p90。
- `modules/style-fingerprint.md` 新增 §2.1 讲解冒号、§2.2 结尾闸门；`modules/style-structure.md` §五 收束规则改为三类禁用收束、§七 禁用闸门新增 3 行；`modules/style-short.md` 自检清单同步。
- 首战（日本左右题）：v2.8 定稿 **3 告警 → v3.3 定稿 0 告警**（详见 `tests/archive/test_japan_2026-09-19.md`）。关键修正：讲解冒号 3→0、结尾由「……吧」号召改为「这年头最不缺的就是结论」（狠话直落）、合并短句使句长均 29.2→33.6、长句率 11.1%→25.0%。
- 回归断言 57 → **63**（新增 6 条 v2.9 规则防漂移断言）。

## v2.8（2026-09-19）· 句法层 + 篇章层闸门

- **起因**：用户在两轮测试后反馈"文风还是不够精确"，v2.7 的 L1 标点层全绿之后，用户进一步指出"**遣词造句还是不像，行文结构也不像**"。用 L1 回测 v2.7 定稿，暴露 4 处 L1 看不见的偏差：**句长均 27.4（基线 35.2）、多逗号率 0%（基线 12.2%）、起笔为设问立论（评论员腔）、论证装置仅 1**。根因：**只校准了标点，没校准句子怎么造、文章怎么搭**。
- 新增 `modules/style-diction.md`（**L2 句法层**）：实测非虚构语料（pin 808 / short 209 / long 252，剔除译稿与小说）建句法基线——句长中位 29/34/40、句长均 35.2/39.3/45.9、长句率 30/39/48%、多逗号率 12.2/13.9/21.8%、句首连接率 10.3/11.2/12.6%、句末反问率 24/23/10%、**引述包抄率 21.1/27.2/24.5%**；句首连接词实测序（所以184·如果177·然而172·当然140·因此138）；修饰标记（解剖动词 0.20/千字、类比 4.2~5.0%）；他的词库与反面清单（零互联网黑话）。
- 新增 `modules/style-structure.md`（**L3 篇章层**）：从语料 300~800 字**真实回答全文**拆出**六种骨架**——A 拆词还原 / B 方法论先行+归谬 / C 否定→荒诞例→正解 / D 让步-转折-类比-否定 / E 自我贬抑起手 / F 反讽归谬+清单；加**链式咬合**推进规则、四种合法起笔、四类合法收束，以及**禁用结构闸门**（首先/其次/综上所述/值得注意的是/一方面…另一方面/末段升华）。
- `scripts/style_check.py` **升级为三层**：L1 标点 + L2 句法（7 指标）+ L3 篇章（起笔判定 / 论证装置计数 / 机械分层标记闸门 / 评价旁白 / 互联网黑话），退出码 0=可交付。
- 首战（同题日本左右）：v2.7 定稿 **4 告警 → v2.8 定稿 0 告警**。关键修正：**句长均 27.4→34.9、多逗号率 0%→14.3%、起笔改为引述包抄、论证装置 1→4（补类比 3 处）**，同时把引述包抄率从过冲的 46% 收回 36%。
- 回归断言 47 → **57**（新增 4 个文件存在性 + 6 条 L2/L3 规则防漂移断言）。

## v2.7（2026-09-19）· 文风指纹闸门（P6）

- **起因**：用户反馈"文风还是不够精确"。实测 1655 条正文（30638 句）建**分层句法基线**（短答/pin/长文三档），发现短答被长文基线误伤，遂分层。
- 新增 `scripts/style_check.py`：对生成文本量逗号/句号/问号/叹号/破折号/省略号/引号密度、句长中位、口癖命中、"我我们"比例，输出偏差与 `告警项 N 处`；**交付前须归零**。
- 新增 `modules/style-fingerprint.md`：分层基线表（短答句长中位 29、破折号仅 0.49、叹号 0.29、引号全角 8.39；"我:我们"≈2.7:1）+ 四种优化方法（基线化 / 真实模式库 / 自检闸门 / A-B 对照）+ 收敛示例。
- 流水线由五步扩为**六步**：新增 `Step 6 过闸（P6）`。
- 首战：日本左右题台词经 **5→3→1→0** 四轮收敛（关键修正：破折号 4.21→0、引号半角→全角、补 6 个口癖、加"我们"）。测试件 `tests/archive/test_japan_2026-09-19.md`。

## v2.6（2026-09-19）· D1 打包修复（用户指定）

- 索引缓存原是派生数据却写在 `scripts/_bm25_index.pkl`，让包体虚胖到 11.33 MB（缓存占 97.8%）。现移出包外——默认 `~/.workbuddy/cache/shifeng-persona-v2/`，文件名按语料路径 sha1 命名；层级 `SHIFENG_CACHE` 环境变量 > 用户缓存目录 > 系统临时目录。包体积 **11.33 MB → 257.3 KB**，首次检索自动重建。
- `retrieve.py` 新增 `cache_dir()` / `index_path()`，以及 `--cache-info`（自检缓存位置）与 `--clean-cache`（清缓存含包内遗留）；`--stats` 增印缓存路径与"在包外"判定。
- `run_regression.py` 旧断言"索引缓存已生成"升级为 **2 条打包守卫**（缓存在包外 / 包内无 `.pkl`），断言总数 46 → **47**。
- 新增 `.gitignore`（`*.pkl` 等），作版本控制与打包双重闸门；`retrieval-guide.md` 补缓存使用说明。
- 验证：`--clean-cache` 清掉旧包内 11.1 MB；删缓存后首次检索正常重建；回归 **47/47**；故意放包内 pkl 探针 → 自检如期 FAIL 并报出文件名。

## v2.5（2026-09-19）· 去元数据自陈

- **起因（用户纠正）**：用户指出「我没有专门谈过」「没系统想过」这类句式"也是 AI 模拟人类时的语言，而不是真实语言"。定性：这是**元数据式自陈**（向用户汇报自己的发言记录/知识状态），与 v2.4 的元叙事开场同族。
- `qc.md` §三·六 新增 **#12 元数据式自陈**（含"行为史陈述 vs 当下状态表达"判据）；失败树 #1/#6/#8/#9、反例黑名单 #5、越界处置、自检清单同步改写。
- **诚实机制从"台词内声明"改为"交付注"**：台词永远不汇报档案（第一人称、只表达当下）；"实体层未覆盖 / 此段为推演非原话"下沉到回答之外的**交付注**。`SKILL.md`《视角铁律》删去"Level D 可短暂用他"的例外——该例外本身即元数据自陈。
- `retrieval-guide.md` ③ 改写：实体/现象两层落差报在交付注，不报在台词。同步修正 `stance_conf.py` TONE 文案。
- **修复 `stance_conf.py` 真 bug（P0）**：`grade()` 末行兜底 `return "D"`，会把"单条 + 单年 + 置信一般"（累计分 s=0）的议题误判为"从未谈过"。已改为 **n≥1 时最低只到 C（留白），D 严格只留给零命中**。

## v2.4（2026-09-19）· 去元叙事开场

- **起因（用户纠正）**：用户指出「我不当多大回事，但顺手把 X 拎出来剖两句」这类开场"没有人会这样说话"——这是 AI 在预告自己要发言（元叙事腔）。
- `qc.md` §三·六 信号表新增 **#11 元叙事开场白**（直接切入正题，第一句就是论点/反讽，绝不预告），并补西贝正反对照样例；时代峰峻测试件删去同款开场。

## v2.3（2026-09-19）· 视角铁律

- **起因（用户纠正）**：用户指出"这个 skill 是扮演石枫不是预测石枫"——此前测试回答误用第三人称"他"代述角色立场（解说员/预测视角）。
- 固化硬规则：正文一律第一人称"我"扮演，禁"他/石枫认为"第三人称代述。落点：`SKILL.md` 新增《视角铁律》+ 禁忌补强 + Step 5 视角要求 + `00-router.md` 负例4 + `style-short.md` 第一人称条款 + `qc.md` §四 视角闸门 + golden J 组回归用例。

## v2.2（2026-09-19）· 降 AI 味 + 高频语汇词表

- **降 AI 味（用户重点反馈）**：`qc.md` 新增 §三·六「反 AI 味自检」——10 条具体信号（解释性旁白 / 平衡中和 / 升华总结 / 泛化主语 / 礼貌缓冲 / 评价式裁判 / 无细节空泛等）→ 改法对照表 + 两对正反对照样例（全球化 / 进步主义）。
- **高频语汇词表 `modules/vocab.md`**：用户指定核心五词（全球化🔴批判 / 古典美德🩷正面有分寸 / 进步主义🔴负面 / 正道世界🩶讽刺 / 现代生活🩶讽刺目田）每条带真实检索例句与态度；另含自造代称（目田/赛里斯/利维坦/秦制/牧田/分配壬）、批判语汇库、正面语汇库。接入 Step 5 流水线。

## v2.1（2026-09-19）· 女娲底稿 + P3/P4/P5

- **挂载女娲底稿**：`references/research/01–06` 并入，补 v2 缺失的深度细节。**已知缺口**：底稿绕过了 v1 的人工裁决记录，无误判红线——涉该主题一律以 `style-short.md` §十 为准。
- **P3 置信度量化**：新增 `scripts/stance_conf.py`。按证据强度（条数 / 跨年反复度 / 分类置信 / satire 混入比）给出 A/B/C/D 等级与对应语气。
- **P4 图谱复用**：新增 `scripts/graph_walk.py`。395 概念 / 303 共现边接入，支持邻接查询与 `--path` 桥接；含译名别名合并（伯克=柏克 等）。
- **P5 回归 CI 化**：新增 `scripts/run_regression.py`（41 项）+ `tests/golden_cases.md`（A–G 组 LLM 输出质量用例）。
- **修复两处 v2.0 回归**：① 知识域边界 7 类清单（误删）→ `qc.md` §三·五；② 人物时间线 / 价值观与内在张力 / 智识谱系（瘦身误砍）→ `persona-context.md`。

## v2.0（2026-09-18）· 检索增强版

- **P0 检索层**：新增 `scripts/retrieve.py`（纯标准库 BM25，多字段加权）。解决静态蒸馏的有损压缩——引用不再靠文档里的记忆 quote，而是实时取回真实原文；satire 判定从人工纪律变为 `category` 标签机器前置。
- **P1 懒加载**：原文风/立场规则拆入 `modules/`，按需只加载命中分支，核心卡瘦身。
- **P2 路由强校验**：新增 `00-router.md`，路由强制显式化，并把两次真实翻车做成负例锚（"如何看待 X"≠长论述；幽默是硬指标）。

## v1.0

初版 6 段式 + 女娲 13 段蒸馏版（见 `../nuwa_shifeng_perspective/` 与 `../nuwa_vs_original_comparison.md`）。三版对比测试见 `../v2_vs_nuwa_test.md`。
